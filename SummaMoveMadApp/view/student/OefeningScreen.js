import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const OefeningScreen = ({ navigation, route }) => {
  return (
    <View>
      <Text>OefeningScreen</Text>
    </View>
  );
};

export default OefeningScreen

const styles = StyleSheet.create({})
